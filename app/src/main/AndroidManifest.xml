<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <queries>
        <package android:name="com.tencent.qqmusiccar" />
        <!-- 或者直接指定 service -->
        <intent>
            <action android:name="com.tencent.qqmusiccar.third.api.IQQMusicApi" />
            <!-- 如果不知道 action，保留 package 即可 -->
        </intent>
    </queries>




    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SudoCar">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:exported="true"
            tools:ignore="DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".receiver.QQMusicBroadcastReceiver" android:exported="true"
            android:permission="TODO">
            <intent-filter>
                <!-- 这里填入你抓到的真实 Action -->
                <action android:name="com.tencent.qqmusiccar.action.META_CHANGED" />
                <action android:name="com.tencent.qqmusiccar.action.PLAYSTATE_CHANGED" />
                <!-- 也可以尝试通用的 Android 媒体广播 -->
                <action android:name="com.android.music.playstatechanged" />
                <action android:name="com.android.music.metachanged" />
            </intent-filter>
        </receiver>
    </application>


<!--    <service-->
<!--        android:name=".service.DataMonitorService"-->
<!--        android:enabled="true"-->
<!--        android:exported="false"-->
<!--        tools:ignore="MissingClass" />-->

</manifest>